packages:
  common:
    brews:
      # Core tools - shared across macOS and Linux
      - neovim
      - tmux
      - fish
      - starship
      - zoxide
      - fzf
      - eza
      - bat
      - ripgrep
      - fd
      - mise
      - lazygit
      - delta
      - difftastic

  darwin:
    casks:
      # macOS-only GUI applications

  linux:
    brews:
      # Linux-only packages (empty for now - uses common)
